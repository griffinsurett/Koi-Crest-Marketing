---
// src/integrations/IntBodyScripts.astro
/**
 * Integration Body Scripts
 *
 * Consolidates all integration scripts that need to run in the <body> section.
 * These scripts can be deferred and don't need to block rendering.
 *
 * Currently includes:
 * - (Add body-level integration scripts here as needed)
 */
---

{/* Add body-level integration scripts here */}
