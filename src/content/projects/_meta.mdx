---
title: "Projects"
heading: "Our recent projects"
description: "Discover our past work in our Portfolio. From websites to digital marketing, view the diverse projects we’ve completed, showcasing our ability to deliver high-quality results across various industries."
hasPage: true
itemsHasPage: false
addToMenu:
  - menu: "main-menu"
  - menu: "links-menu"
---
import ContentRenderer from '@/components/ContentRenderer/ContentRenderer.astro';
import { query, sortByOrder } from '@/utils/query';

    <ContentRenderer
      query={query("projects").orderBy(sortByOrder())}
      variant="PortfolioVariant"
      columns={3}
      gap="lg"
    />

    <ContentRenderer
      query={query("testimonials").orderBy(sortByOrder())}
      variant="TestimonialsVariant"
      columns={2}
      gap="lg"
      />